- name: Copy a file
  hosts: all
  tasks:
   - name: Copy file with owner and permissions
     ansible.builtin.copy:
       src: /tmp/bajaj/abhi
       dest: /tmp/xyz
       owner: root
       group: root
       mode: '0700'
       delegate_to: localhost

